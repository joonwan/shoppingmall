spring:
#  datasource:
#    url: jdbc:h2:mem:shoppingmall
#    username: sa
#    password: sa

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true

logging:
  level:
    org.hibernate.sql: trace